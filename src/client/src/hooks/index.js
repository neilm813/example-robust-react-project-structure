/*
Shared custom hooks that multiple components could use.
Custom hooks that are only for a specific component can be placed in that
component's folder instead.
*/

export { useLocalStorage } from './useLocalStorage';
